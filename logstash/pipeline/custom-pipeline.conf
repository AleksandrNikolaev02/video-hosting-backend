input {
  jdbc {
    schedule => "*/10 * * * * *"
    jdbc_connection_string => "jdbc:postgresql://db:5432/business"
    jdbc_user => "postgres"
    jdbc_password => "1234"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_driver_library => "/usr/share/logstash/postgresql-42.7.5.jar"
    statement => "SELECT id, description, name, created_at, path FROM videos WHERE status = 'UPLOADED'"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "videos"
    document_id => "%{id}"
    doc_as_upsert => true
  }
}